============
Installation
============

At the command line::

    $ easy_install kipart

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv kipart
    $ pip install kipart
